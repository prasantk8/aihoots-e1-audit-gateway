# LATER — ideas parked, not pursued (anti-drift rule #4)

One line each. No code, no PRDs, no domains until the current episode ships.

- E-next: **Governing OpenClaw** — put a *generic, home-installed* OpenClaw agent behind
  the Audit Gateway; run a documented adversarial battery (MITRE ATLAS/ATT&CK-derived cases)
  with/without the gateway; publish before/after defense rates as honest numbers.
  Provenance rule: designed ONLY from public security literature + a fresh local install.
  Never a re-creation of any employer system.
- Signed audit records (per-record digital signatures) as an ADR-001 upgrade episode.
- Object-lock / WORM storage integration for the audit log.
- Model report cards: benchmark quantized SLMs on governance-relevant tasks.
